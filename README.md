📌 README – Sistema de Portaria TechZone
📖 Descrição

O Sistema de Portaria - TechZone é uma atividade desenvolvida em JavaScript com o objetivo de simular o controle de acesso de pessoas autorizadas em determinado horário.

O sistema verifica:

✅ Se o nome do usuário está na lista de autorizados

⏰ Se o horário está dentro do permitido (antes das 22h)

🚫 Caso contrário, o acesso é negado

O projeto foi desenvolvido utilizando apenas HTML e JavaScript, executando diretamente no navegador.

🎯 Objetivo da Atividade

Compreender na prática:

O que é um array

Como utilizar o método .includes()

Como trabalhar com:

prompt()

alert()

Estrutura condicional if / else

Como converter texto para número usando Number()

Lógica de decisão baseada em múltiplas condições

⚙️ Funcionamento do Sistema

O sistema possui uma lista de usuários autorizados

Ao abrir a página:

O sistema solicita o nome do usuário.

Em seguida, solicita a hora atual (de 0 a 23).

O sistema verifica:

Se o nome está na lista.

Se o horário é menor que 22.
